<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/campaign_img"
        android:layout_width="match_parent"
        android:layout_height="198dp"
        android:scaleType="centerCrop"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp" />

    <TextView
        android:id="@+id/campaign_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/campaign_img"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:padding="16dp"
        android:text="ReBag: a fashional way of recycling with 35% exclusive discount!"
        android:textSize="14sp"
        android:textColor="@color/primary_text" />

    <TextView
        android:id="@+id/campaign_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:text="We make designer's bags and accessories, all converted from brand new apparels, to make you effortless stand out and contribute to the environment. Exclusive discount code : REBAGNOW for 35% off. "
        android:textSize="10sp"
        android:textColor="@color/secondary_text"
        android:lineSpacingMultiplier="1"
        android:lines="3"
        android:ellipsize="end"
        android:layout_below="@+id/campaign_title" />

    <!--<ImageView-->
        <!--android:id="@+id/campaign_divider"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="24dp"-->
        <!--android:src="@drawable/ic_divider"-->
        <!--android:layout_below="@+id/campaign_description" />-->

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider"
        android:layout_below="@+id/campaign_description"
        android:layout_marginTop="16dp" />
</RelativeLayout>